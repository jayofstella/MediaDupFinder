# Changelog

## 1.7.0 - 2026-08-20

- 光盘通用文件名改为上下文识别：`VTS_01_1.VOB`、`00001.M2TS` 等不再直接充当作品名，而是读取最近的有效上级影片目录，并保留光盘/分段编号。
- 新增“长系列名 + 连续编号”和括号单集编号解析；`fellatiojapan567/568`、`Japanese amateur leak (1)/(10)` 会作为不同具体作品隔离。
- `VD-1080`、`HD-720`、`FHD-1080` 等明确归为画质标签，短 ASCII 碎片（如 `ck`）不再成为可靠作品身份。
- 番号后的长描述文件名也会保留末尾 `_1/_2/_3/_4` 分段，避免把同一作品的不同片段当成可互删版本。
- 重写作品身份与 MD5 结果合并：精确 MD5 组成为不可跨越的内容边界，名称候选不能再传递串联多个不同 MD5 组。
- 新增组内“内容关系”与候选组“MD5 子组”列，清楚区分作品身份相似、快速指纹不同、完整 MD5 相同和组内 MD5 不同。
- 结果增加“身份来源”，可以审计作品身份来自文件名还是上级影片目录；完整信息窗口和 CSV 同步输出。
- 新增最大文件大小、自定义扩展名和批量排除目录；与最小文件大小、关键字、隐藏/临时项及比对范围一起在扫描前配置并自动保存。
- 候选组和组内文件的全部列均可点击排序，并新增“列顺序”窗口，支持任意列向左/向右移动并保存布局。
- 自动化测试扩充到 114 项，覆盖文档截图中的通用光盘名、画质误识别、系列编号、长文件名分段、MD5 桥接、大小上限、排除目录与列布局。

## 1.6.0 - 2026-08-19

- 新增可编辑的最小文件大小筛选，单位为 MB；填写 `0` 表示不限制，小于阈值的文件在作品解析和 MD5 读取前直接跳过。
- 新增四种比对范围：全部文件、仅不同所在目录、仅同一所在目录、仅不同扫描根目录/盘符；范围同时约束作品身份与 MD5 通道。
- 作品身份比对现在可单独关闭，可配合智能或完整 MD5 执行“仅字节级重复”扫描；程序会阻止作品身份和 MD5 同时关闭。
- 新增隐藏/系统文件与目录过滤、临时或未完成下载文件过滤，以及自定义文件名关键字排除。
- 扫描进度与完成摘要显示保留数量、筛除数量和筛除原因；全部新选项会自动保存并在下次启动时恢复。
- 文件类型选项现在也会保存，不再在重启后固定恢复为视频模式。
- 自动化测试扩充到 95 项，覆盖大小阈值、过滤原因、范围隔离、MD5 范围分桶和设置持久化。

## 1.5.1 - 2026-08-19

- 修复 GitHub Actions Windows Runner 同时使用 8.3 短路径和完整用户目录名时产生的测试误报；测试现在按实际目录身份判断，不再要求路径文本完全相同。
- 此问题仅影响自动构建测试，不影响 v1.5.0 的作品身份识别与扫描逻辑。

## 1.5.0 - 2026-08-19

- 核心匹配由“文件名模糊相似”改为“解析并核对作品身份”，身份类型包括作品编号、日期单集、季集编号和影视标题。
- 识别 `YY.MM.DD`、`YYYY.MM.DD`、`YYYYMMDD` 日期作品，以及 `S02E07`、`2x07`、`E07`、`第12集` 等单集标识；同系列不同日期或不同集强制隔离。
- 修复 `2048社区 - fun2048.com@fc...` 类站点包装误合并：删除站点品牌与域名，但保留并比较 `fc...` 唯一标识。
- 番号后的独立数字及 `A/B` 后缀作为分段标记；例如 `KAVR-253-2` 与 `KAVR-253-8` 不再成为可互删候选。
- 普通标题的模糊容错改为逐词检查；共享长系列前缀但包含不同人物、日期或单集词语时不再得到高置信度。
- `015`、`032`、`230` 等缺少系列名的短纯数字不再单独作为可靠作品身份。
- MD5 模式完全由扫描前的下拉选项决定；扫描过程中不再弹出二次确认，也不会因无人值守而暂停。
- 候选组与组内文件均支持点击列标题排序；大小、时长、分辨率和修改时间按真实数值排序，未知值固定置后。
- 结果表、完整信息窗口和 CSV 增加识别作品、身份类型、作品身份键、分段、日期/季集、时长差、编码等审计字段。
- 片长安全门槛收紧：差异超过 3% 且超过 30 秒时进入重点复核，并禁止智能批量标记删除。
- 自动化测试扩充到 83 项，覆盖用户报告的系列日期、站点前缀、分段、长共同前缀和扫描行为回归。

## 1.4.0 - 2026-08-19

- 文件名匹配改为四个可见阶段：建立索引、生成候选、相似度比对和整理候选组。
- 相似度阶段显示精确的候选比对总数、已完成数量、剩余数量和百分比。
- 新增比对速度、已用时间、预计剩余时间，以及当前正在核对的两个文件名。
- 进度回调限制为每 200 毫秒最多刷新一次，避免频繁更新界面拖慢大规模扫描。
- “停止”按钮现在覆盖文件名索引、候选生成、相似度比对及候选组整理全过程。
- 候选组整理按待处理文件数显示进度，较大的连锁候选组也不会只停留在静态提示。
- 自动化测试扩充到 64 项。

## 1.3.0 - 2026-08-19

- “添加目录”升级为批量扫描位置选择窗口，可一次选择多个 Windows 磁盘盘符。
- 支持一键选择全部固定磁盘，并区分固定磁盘、移动磁盘、网络驱动器和光驱。
- 同一窗口可累计添加普通文件夹，或粘贴每行一个的多目录路径。
- 自动清理资源管理器复制路径中的英文引号，并对已有目录与本次选择去重。
- 确认时检查盘符和目录可访问性，空光驱或离线盘可以提示后跳过。
- 主界面扫描位置列表加高并增加横向滚动条。
- 自动化测试扩充到 61 项。

## 1.2.2 - 2026-08-19

- 修复 Windows EXE 启动时出现“attempted relative import with no known parent package”的问题。
- PyInstaller 改为使用项目顶层启动器，冻结环境不再依赖源码目录结构。
- 包内 `__main__` 改用绝对导入，并增加两种启动入口的回归测试。
- GitHub Actions 和本地构建脚本会实际启动打包后的 EXE 执行无界面自检，异常产物不再上传。
- 修正文档中的当前维护版本。
- 自动化测试扩充到 58 项。

## 1.2.1 - 2026-08-19

- 扫描结果表增加视频编码与 MD5 状态列。
- 文件行增加右键菜单：查看完整信息、打开文件、资源管理器定位、Windows 文件属性、复制路径和复制文件名。
- 右键菜单可以直接设置保留、标记删除或撤销标记，并保留已有多选操作。
- 新增完整文件信息窗口，集中显示文件系统、媒体、完整 MD5、名称解析和候选组判定信息。
- 完整信息窗口支持复制全部内容、打开文件和定位文件。
- 自动化测试扩充到 54 项。

## 1.2.0 - 2026-08-19

- 将 MD5 设置升级为“关闭 / 智能扫描 / 完整扫描”三档，默认使用智能扫描。
- 智能模式先按大小筛选，再读取大文件开头、中间、结尾各 1 MB 生成快速指纹；仅对指纹相同者计算完整 MD5。
- 小于等于 3 MB 的文件在快速阶段一次完成 MD5，避免重复读取。
- 新增本地 MD5 缓存；复用前校验路径、大小、时间状态和新鲜快速指纹。
- 同大小候选的完整读取上限达到 20 GB 时，先显示扫描量与时间区间，可继续、跳过 MD5 或停止扫描。
- 哈希阶段按实际读取容量显示百分比、吞吐速度和预计剩余时间。
- 扫描完成后显示 MD5 实际读取量与缓存命中数，CSV 增加 MD5 状态字段。
- 自动迁移 v1.1 的 MD5 开关设置，不影响已有用户偏好。
- 自动化测试扩充到 53 项。

## 1.1.0 - 2026-08-19

- 清理网站域名、`@发布组`、RARBG/YTS 等常见发布组标记。
- 识别国语中字、国粤双语、简繁中字等复合语言字幕标签。
- 内置常见繁体到简体匹配映射，并统一尾部罗马数字序号。
- 发行年份成为安全别名：单侧年份可匹配，双侧不同年份强制隔离。
- 番号前导零统一，并支持 1PONDO、10MUSUME、Caribbeancom、Tokyo-Hot 和字母数字混合前缀。
- 英文标题支持词语顺序变化匹配。
- 新增文件大小预筛 + 完整 MD5 扫描，可发现文件名完全不同的字节重复文件。
- 合并 MD5 与文件名结果，确保同一文件不会出现在多个候选组。
- 视频时长接近时显示辅助信息；片长差异过大时禁止智能批量标记删除。
- 删除前重新校验文件大小和修改时间，MD5 过程中检测文件状态变化。
- CSV 报告增加识别依据、MD5、片长提示和重点复核字段。
- 自动化测试扩充到 45 项。

## 1.0.0 - 2026-08-19

- 初始公开版本。
- 多目录后台扫描与视频/全部文件模式。
- 作品编号、中文片名、发布标签归一化与模糊分组。
- 分段文件保护、差异编号保护和锚点拆组。
- 中文 Tkinter 桌面界面。
- 手工/智能保留删除标记、回收站操作和 CSV 导出。
- 可选 ffprobe 元数据读取。
- 15 项核心自动化测试。
- Windows x64/x86 GitHub Actions 构建与标签发布流程。

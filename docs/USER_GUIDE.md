# 用户使用指南

## 第一次启动

1. 下载并解压发布 ZIP。不要直接在压缩包预览窗口内运行程序。
2. 双击 `MediaDupFinder.exe`。
3. Windows SmartScreen 若提示“Windows 已保护你的电脑”，这是未购买代码签名证书的开源软件常见提示。确认文件来自项目官方 Release 后，可点击“更多信息”查看发布者和文件名，再决定是否运行。

## 扫描

1. 点击“添加目录”，可以连续添加多个硬盘或文件夹。
2. “包含子目录”默认开启。
3. 相似度建议先用“标准（推荐）”：
   - 保守：候选更少，误报概率较低。
   - 标准：适合大部分整理任务。
   - 宽松：召回更多候选，需要更仔细复核。
4. 类型默认是“视频文件”。只有确实要整理图片、文档或压缩包时才切换“全部文件”。
5. MD5 默认选择“智能扫描（推荐）”：
   - 关闭 MD5：只做文件名和可选视频信息识别，速度最快。
   - 智能扫描：同大小预筛后，大文件只读取开头、中间、结尾各 1 MB；快速指纹相同才计算完整 MD5。
   - 完整扫描：对全部同大小候选进行完整 MD5 校验，适合希望彻底检查且可以等待的情况。
6. 点击“开始扫描”。扫描阶段不会改名、移动或删除任何文件。

唯一大小的文件不会进行哈希读取。若同大小候选的完整读取上限达到 20 GB，软件会先说明文件数量、读取容量和粗略时间区间：“是”继续，“否”只跳过本次 MD5 并保留文件名结果，“取消”停止扫描。

哈希期间底部会显示当前阶段、读取百分比、已读容量、速度和预计剩余时间。已经计算过的 MD5 会保存在本机缓存中；下次复用前仍会重新检查文件状态和快速指纹。

## 检查候选组

左侧每行是一组候选文件，“依据”分为文件名、MD5、混合三类，并显示数量、估计可释放空间和最低匹配置信度。右侧显示组内文件：

- 文件名与完整目录
- 文件大小和格式
- 分辨率、时长、修改时间
- MD5（智能模式下只为快速指纹相同者及小文件生成；完整模式覆盖全部同大小候选）
- 当前决定与重点复核提示

右侧表格还显示视频编码与 MD5 状态，可以使用底部横向滚动条查看完整列。双击一行，或点击“在资源管理器中查看”，可以定位原文件并手工播放检查。

在任意文件行上点击鼠标右键，可以：

- 查看完整文件信息
- 直接打开文件
- 在 Windows 资源管理器中选中文件
- 打开 Windows 文件属性
- 复制一个或多个文件的完整路径或文件名
- 直接设置保留、标记删除或撤销标记

“查看完整文件信息”窗口会集中显示完整路径、精确字节数、文件状态、修改/创建/访问时间、分辨率、时长、编码、完整 MD5、MD5 来源、名称归一化结果、匹配原因、置信度和重点复核提示。窗口中的“复制全部信息”适合在反馈误识别问题时使用。

## 做出决定

- “设为保留”：一组内选择一个明确需要留下的文件。
- “标记删除”：可多选；软件会阻止把整组全部标记删除。
- “撤销标记”：恢复为未决定。
- “当前组智能选择”：按分辨率、封装格式和大小给出建议。
- 菜单“操作 → 全部组智能选择”：一次标记全部候选组，但仍不会执行删除。

智能选择只是建议。例如，同为 1080P 时，大文件未必一定更好；不同剪辑版、配音版、字幕版也可能都值得保留。如果读取到的片长差异过大，智能选择只建议一个保留项，不会自动把其他文件标记删除。

## 执行删除

1. 检查所有红色“删除”标记。
2. 点击“执行已标记删除”。
3. 阅读文件数量、总大小、预览清单和片长差异警告。
4. 再次确认后，文件才会进入 Windows 回收站。

程序不提供批量永久删除按钮。需要恢复时，请立即打开 Windows 回收站。网络共享路径因无法保证可恢复，会被拒绝。

## 读取真实分辨率

没有 `ffprobe.exe` 时，软件仍可正常工作，但只会根据文件名中的 `4K`、`2160P`、`1080P` 等标签推测分辨率。

如需读取媒体内部信息，把兼容系统的 `ffprobe.exe` 放在主程序同一目录，重启后勾选“读取候选视频分辨率”。详细说明见项目 `tools/README.md`。

## 导出报告

点击“导出报告”可保存 CSV。文件使用 UTF-8 BOM，中文 Windows 上可直接用 Excel 打开。报告包含识别依据、匹配原因、MD5、MD5 状态（完整计算、缓存复用或快速跳过）、片长保护提示、置信度、决定、完整路径、大小和分辨率，可作为删除前审核记录。
